1. Install the AgentKit API Tool using pip: pip install agent-kit-api-tool
2. Import the necessary modules in your Python script: from agent_kit import run_test
3. Run the tests using the run_test function: run_test(test_id)
4. Use the API endpoint to retrieve test results: curl http://localhost:8080/agent-kit-api/{test_id}